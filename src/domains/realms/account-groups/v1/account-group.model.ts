// Re-export all from latest
export * from '@/domains/realms/account-groups/latest/account-group.model';

// Override specific types if needed
// Example: export type AccountGroup = CustomAccountGroup;
