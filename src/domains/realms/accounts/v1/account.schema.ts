// Re-export all from latest
export * from '@/domains/realms/accounts/latest/account.schema';
export type { AccountResetPassword } from '@/domains/realms/accounts/latest/account.schema';

// Override specific schemas if needed
// Example: export const accountCreateSchema = customSchema;
