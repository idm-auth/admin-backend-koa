// Re-export all from latest
export * from '@/domains/config/latest/webAdminConfig.model';

// Override specific methods if needed
// Example: export const getWebAdminConfig = customGetWebAdminConfig;
